{
  "name": "deepseek-time",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/stage-adapters.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/stage-adapters.mjs --check && node --test tests/*.test.mjs"
  }
}
